# -----------------------------------------------------------------------------
# jdlengine - sandbox application CMakeLists
# -----------------------------------------------------------------------------
set(APP_NAME sandbox)

add_executable(${APP_NAME} Main.cpp)
